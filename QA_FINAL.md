

## Final enhancement round — audit filters, subscription visibility, bulk keys

تم توصيل مرشحات سجل التدقيق عبر tRPC حسب نطاق التاريخ ونوع الإجراء واسم المسؤول، مع تحقق خادمي من صيغة التاريخ وحدود طول النص، وبقاء الإجراء خلف صلاحية `audit.read`. أضيفت واجهة مرشحات متجاوبة وزر مسح، مع حالة فارغة واضحة.

أضيف إلى حساب العميل مؤشر مرئي للمدة المتبقية يعتمد على `startsAt` و`endsAt` ويعرض عدد الأيام وتاريخ الانتهاء، إضافة إلى سجل المفاتيح المستخدمة الذي يعرض `keyHint` فقط ولا يعرض المفتاح الخام أو بيانات عميل آخر.

أصبح إنشاء دفعة المفاتيح في لوحة المالك ينزّل CSV تلقائياً بعد نجاح الإنشاء، مع بقاء الحد الأقصى 100 مفتاح وارتباط كل مفتاح بالخطة وتحديث الإحصائيات وسجل التدقيق.

نتيجة التحقق: `pnpm exec tsc --noEmit` ناجح، `pnpm build` ناجح، وVitest: 10 ملفات ناجحة و34 اختباراً ناجحاً، مع اختبار تكامل قاعدة بيانات واحد متخطى عند غياب `TEST_DATABASE_URL` المعزول. تم التقاط معاينتي `/account` و`/owner` بصرياً على سطح المكتب، وظهرت المكونات الجديدة متناسقة مع تصميم Hybrid Glass الداكن. تحذير Vite المتعلق بمسار أصل الصورة يبقى تحذيراً وقت البناء ولا يمنع البناء أو التشغيل؛ كما أن تحذير حجم الحزمة ليس فشل تجميع.

لا يُدّعى اختبار قاعدة بيانات إنتاجية أو اختبار دفع خارجي فعلي دون أسرار/قاعدة اختبار معزولة. هذه القيود موثقة ولا تمنع وظائف نموذج البيع اليدوي الحالي عبر WhatsApp ومفاتيح التفعيل.

### الجولة: 17 أغسطس 2026

- TypeScript: ناجح.
- Production build: ناجح.
- Vitest: 34 ناجحاً، 1 متخطى لغياب قاعدة اختبار معزولة.
- Visual QA: `/account` و`/owner` على سطح المكتب ناجحتان.
- Dev server: يعمل بعد إعادة التشغيل.

## الجولة الأخيرة — البحث المتقدم والتحذير والترقيم

تم تنفيذ شريط بحث عام في سجل التدقيق، موصول بخادم tRPC مع تحقق Zod وحد أقصى لطول الإدخال، ويبحث في الإجراء ونوع المورد والمعرف واسم أو بريد المستخدم، مع بقاء صلاحية `audit.read` مطبقة خادمياً.

تم تحسين مؤشر مدة الاشتراك ليستخدم حالة تحذيرية ديناميكية عندما تقل المدة المتبقية عن سبعة أيام، مع الحفاظ على حالة الانتهاء وحالات الانتظار والاشتراك النشط، وترجمة الحالات عبر اللغات الخمس.

تمت إضافة ترقيم صفحات خادمي لسجل المفاتيح المستخدمة، مع إجمالي النتائج، وحدود آمنة للصفحة وحجم الصفحة، وأزرار السابق/التالي وحالات التعطيل والفراغ.

نتائج التحقق:

- TypeScript: ناجح.
- Vitest: 34 اختباراً ناجحاً، واختبار تكامل قاعدة بيانات واحد متخطى لغياب `TEST_DATABASE_URL` معزول.
- Production build: ناجح عبر Vite وesbuild.
- التحقق البصري: ناجح لصفحة الحساب ولوحة المالك على سطح المكتب؛ لم تظهر أخطاء TypeScript أو LSP.
- ملاحظة البناء غير الحرجة: تحذير قائم عن أصل تخزين خارجي وقطعة JavaScript كبيرة، ولا يمنع البناء أو التشغيل.

## الجولة الأخيرة — تصدير التدقيق والنطاقات الزمنية وLoading Skeleton

أضيف زر لتصدير سجل التدقيق الحالي إلى CSV بعد تطبيق المرشحات، ويشمل الملف العملية ونوع المورد ومعرفه والمسؤول والنتيجة والتوقيت بصيغة UTC. يعتمد التصدير على البيانات المفلترة التي يمنحها الخادم، مع تعطيل الزر عند عدم وجود نتائج، وتهريب قيم CSV لمنع كسر الأعمدة أو إدخال صيغ غير مقصودة.

أضيفت خيارات نطاق زمني جاهزة لآخر 7 أيام وآخر 30 يوماً وآخر 90 يوماً، مع خيار مخصص يحافظ على حقلي تاريخ البداية والنهاية الحاليين. تبقى التصفية النهائية والتحقق من التاريخ على الخادم عبر عقد سجل التدقيق.

أضيف Loading Skeleton متحرك لسجل المفاتيح المستخدمة أثناء جلب صفحة جديدة، مع الحفاظ على حالة الجدول الفارغة وأزرار الترقيم بعد اكتمال الطلب.

نتائج التحقق:

- TypeScript: ناجح.
- Vitest: 36 اختباراً ناجحاً، واختبار تكامل قاعدة بيانات واحد متخطى لغياب `TEST_DATABASE_URL` معزول.
- Production build: ناجح.
- Visual QA: صفحات `/owner` و`/account` ناجحتان على سطح المكتب، مع الحفاظ على تصميم Hybrid Glass والاستجابة الأساسية.
- ملاحظات غير حرجة: تحذير أصل الصورة الخارجي وتحذير حجم الحزمة ما زالا موجودين ولا يمنعان البناء أو التشغيل.

## الجولة الحالية — مسح المرشحات وحفظها وتصدير PDF

أضيف زر لمسح جميع مرشحات سجل التدقيق وإعادة العرض الافتراضي فوراً. تُحفظ إعدادات البحث والتصفية تلقائياً في تخزين المتصفح بمفتاح مرتبط بمعرف المسؤول، وتُستعاد عند تسجيل الدخول لاحقاً دون مشاركة الإعدادات بين المسؤولين. أضيف تصدير PDF منسق لسجل التدقيق المعروض، يتضمن عنوان التقرير وتاريخ التصدير والعملية والمسؤول والمورد والنتيجة والتاريخ، مع تقسيم تلقائي للصفحات.

نتائج التحقق: `pnpm exec tsc --noEmit` ناجح، وVitest: 10 ملفات ناجحة و36 اختباراً ناجحاً، مع اختبار تكامل قاعدة بيانات واحد متخطى عند غياب `TEST_DATABASE_URL` المعزول. `pnpm build` ناجح. تمت معاينة صفحة Owner بصرياً على سطح المكتب، وظهرت أزرار CSV وPDF ومسح جميع المرشحات ضمن التخطيط الداكن المتجاوب. لم يُشغّل اختبار قاعدة بيانات حقيقي على قاعدة الإنتاج.

## الجولة الحالية — تقرير PDF المميز ومزامنة المرشحات خادمياً

تمت ترقية تقرير سجل التدقيق ليضم هوية DAR.EST في الترويسة، وملخصاً افتتاحياً يوضح إجمالي السجلات والمرشحات المطبقة، وترقيم الصفحات والتذييل. نُقلت تفضيلات المرشحات من التخزين المحلي إلى جدول خادمي مرتبط بمعرف المسؤول، مع إجراءات tRPC محمية بصلاحية `audit.read` والتحقق من المدخلات، بما يسمح باستعادتها عبر الجلسات والأجهزة دون خلط الحسابات.

نتيجة التحقق: TypeScript ناجح، وVitest ناجح بـ36 اختباراً مع اختبار تكامل حقيقي واحد متخطى عند غياب `TEST_DATABASE_URL` المعزول، وبناء الإنتاج ناجح، والتحقق البصري لصفحة المالك على سطح المكتب ناجح. تحذيرات البناء الحالية غير مانعة: أصل صورة تخزين خارجي يُحل وقت التشغيل وحجم حزمة الواجهة يتجاوز عتبة التنبيه.

## الجولة الأخيرة — تحسين قائمة اللغات والأيقونات ومزايا الخطط

تم إصلاح الفاصل النحوي المفقود بين تعريفَي الروسية والأوكرانية في `client/src/pages/Home.tsx`، وأصبح Vite يبني بنجاح. تم تحسين قائمة اللغات المكتبية والهاتفية بألوان Hybrid Glass وحالة نشطة وأيقونات مناسبة. استُبدل الوصف العام المتكرر في بطاقات الخطط بقوائم مزايا عملية ومختلفة لكل خطة باللغات الخمس، مع أيقونات ونصوص وصول واضحة.

نتائج التحقق: `pnpm exec tsc --noEmit` ناجح، `pnpm build` ناجح، Vitest: 37 اختباراً ناجحاً مع اختبار تكامل قاعدة بيانات واحد متخطى لغياب `TEST_DATABASE_URL` المعزول. تم تشغيل خادم التطوير بنجاح والتقاط معاينة سطح المكتب للصفحة الرئيسية بعد الإصلاح. تبقى تحذيرات أصل الصورة الخارجي وحجم الحزمة غير مانعة للبناء أو التشغيل.

## الجولة الحالية — توحيد رقم التواصل وإصلاح مرجع Home
تم توحيد رقم WhatsApp في واجهة الصفحة الرئيسية وصفحة Checkout ولوحة Owner واختبار العقد إلى `+972543350466`، مع استخدام الصيغة الرقمية `972543350466` داخل رابط WhatsApp. أظهر البحث في ملفات المصدر الرقم الجديد فقط ضمن مواضع التواصل، ولم تظهر الصيغة القديمة ذات الفواصل. كما تم التحقق من أن مرجع `PlanIcon` معرف داخل `Home.tsx`.

نتائج التحقق:

- TypeScript: ناجح عبر `pnpm exec tsc --noEmit`.
- Vitest: ناجح وفق الجولة الحالية، مع بقاء اختبار قاعدة البيانات الحقيقي مشروطاً بوجود `TEST_DATABASE_URL` معزول.
- Production build: ناجح عبر Vite وesbuild.
- ملاحظات غير حرجة: تحذير أصل الصورة الخارجي وحجم chunk ما زالا تحذيرين وقت البناء ولا يمنعان التشغيل.

## الجولة الحالية — بوابة الاشتراك وحفظ الحساب بعد تسجيل الخروج
أضيفت دالة خادمية موحدة للتحقق من أن الاشتراك حالته `active` وأن تاريخ انتهائه في المستقبل، مع عقد `account.access` لإرجاع حالة الوصول التشغيلية. أصبحت لوحة الحساب تعتبر الاشتراك منتهياً فعلياً عند تجاوز `endsAt` حتى لو لم تُحدّث قيمة الحالة بعد، وتعرض حالة انتظار/منع بدلاً من حالة نشطة. منطق تسجيل الخروج الحالي يمسح جلسة الدخول فقط ولا يحذف المستخدم أو الاشتراك أو المفاتيح، لذلك يمكن تسجيل الدخول مجدداً واستعادة البيانات عند وجود اشتراك صالح.

التحقق:

- TypeScript: ناجح عبر `pnpm exec tsc --noEmit`.
- Vitest: 39 اختباراً ناجحاً، واختبار قاعدة البيانات الحقيقي متخطى لغياب `TEST_DATABASE_URL` معزول.
- Production build: ناجح عبر Vite وesbuild.
- تم الحفاظ على تدفق Checkout وتفعيل المفتاح قبل الوصول التشغيلي؛ لا توجد حالياً شاشة مستقلة لإنشاء شركة متعددة المستخدمين، لأن النموذج الحالي يستخدم حساب OAuth واحداً كحد الشركة، كما هو موثق في `docs/tenant-isolation.md`.

## الجولة الحالية — الشركات متعددة المستخدمين وبطاقة حالة الاشتراك

تمت إضافة جداول `companies` و`company_members` مع مالك الشركة وأدوار الأعضاء وقيد فريد يمنع تكرار العضو داخل الشركة. أضيفت إجراءات خادمية لإنشاء الشركة وإدارة الأعضاء، مع اشتراط اشتراك فعال وفحص صلاحيات الأدوار `owner/admin/manager/member/viewer` وعزل العضوية على مستوى الشركة.

تمت إضافة واجهة داخل `/account` تعرض حالة الاشتراك والخطة وتاريخ الانتهاء والمدة المتبقية وشريط التقدم، مع واجهة الشركة وإدارة الأعضاء للمستخدم المصرح له. تم التحقق بصرياً على سطح المكتب والهاتف.

نتائج التحقق: `pnpm drizzle-kit generate` أفاد بعدم وجود تغييرات مخطط جديدة بعد تطبيق migration الشركات؛ `pnpm exec tsc --noEmit` ناجح؛ `pnpm test` نجح بـ12 ملف اختبار و42 اختباراً ناجحاً، مع اختبار تكامل حقيقي واحد متخطى لغياب `TEST_DATABASE_URL` المعزول. تم إصلاح imports مفقودة في `drizzle/schema.ts` و`server/companyPermissions.ts`.

## الجولة الحالية — دعوات الأعضاء وتجديد الاشتراك وسجل نشاط الشركة

تمت إضافة دعوات شركات أحادية الاستخدام مع تخزين الهاش فقط، انتهاء صلاحية وحالة دعوة، قبول خادمي مرتبط بالمستخدم، وإلغاء الدعوة. أضيفت صفحة `/invite/:token` لقبول الدعوة، وواجهة الحساب تعرض الدعوات المعلقة وتوفر نسخ الرابط وتسليم البريد عبر `mailto` دون كشف الهاش المخزن.

أضيفت واجهة طلب تجديد أو ترقية الاشتراك للخطة الشهرية أو الفصلية أو نصف السنوية أو السنوية، مع إعادة استخدام تدفق الدفع اليدوي ومراجعة المالك. كما أضيف سجل نشاط الشركة المقيد بالشركة والصلاحيات، ويعرض العضو والعملية والكيان والتوقيت.

نتائج التحقق: TypeScript ناجح، Vitest: 42 اختباراً ناجحاً واختبار قاعدة بيانات حقيقي واحد متخطى لغياب `TEST_DATABASE_URL` المعزول، وبناء الإنتاج ناجح. تمت معاينة `/account` و`/invite/example-token` بصرياً على سطح المكتب. تحذيرا أصل الصورة الخارجي وحجم الحزمة غير مانعين للبناء.

## الجولة الحالية — مؤشر حالة طلب التجديد أو الترقية
تمت إضافة مؤشر مرئي داخل صفحة Account يعرض حالة آخر طلب مراجعة يدوية: تم إرسال الطلب، قيد المراجعة، معتمد، مرفوض، أو يحتاج إجراء. أضيفت حالة تحميل أثناء الإرسال، وتحديث تلقائي لقائمة الطلبات بعد نجاح الإرسال، مع رسائل مترجمة بالعربية والإنجليزية والعبرية والروسية والأوكرانية ودعم RTL/LTR. لم يتغير تدفق الدفع اليدوي أو صلاحيات الاعتماد.

نتائج التحقق: `pnpm exec tsc --noEmit` ناجح دون أخطاء، وVitest ناجح بعدد 42 اختباراً مع اختبار قاعدة البيانات الحقيقي متخطى بشكل مقصود عند غياب `TEST_DATABASE_URL` المعزول، و`pnpm build` ناجح. بعد إعادة تشغيل خادم التطوير ظهر `Server running` دون أخطاء جديدة؛ أخطاء TransformError الأقدم في السجل تعود إلى ما قبل إعادة التشغيل وليست نتيجة الجولة الحالية.

## الجولة الحالية — إعادة تصميم قسم خطط الاشتراك
تم تحديث قسم الخطط فقط داخل `Home.tsx` إلى أربع بطاقات مميزة: Basic لشهر، Plus لثلاثة أشهر، Pro لستة أشهر، وVIP لسنة. أضيفت مزايا مختلفة لكل خطة، شارات «وفر أكثر» و«الأكثر اختياراً» و«أفضل قيمة»، وتفاصيل التوفير مقارنة بالدفع الشهري، مع أزرار «اشترك الآن» والحفاظ على تدفق اختيار الخطة.

تم التحقق بصرياً على سطح المكتب والهاتف، مع دعم اللغات الخمس وRTL/LTR وعدم تعديل الأقسام الأخرى. TypeScript وVitest والبناء ناجحة؛ سجل الخادم الحالي يثبت أن خطأ schema القديم انتهى وأن الخادم يعمل، بينما تحذير `baseline-browser-mapping` غير مؤثر.

## الجولة الحالية — لوحات CRUD للوحدات المستعادة
تمت إضافة واجهات تشغيلية كاملة داخل Workspace للمستأجرين والصيانة والعقود والمدفوعات التشغيلية، تشمل نماذج إضافة وتعديل وحذف، حالات فارغة، أزرار تحميل، ورسائل خطأ، مع بقاء الإجراءات خلف عزل الشركة وصلاحيات Workspace وحدود الاشتراك حيث تنطبق.

نتائج التحقق: TypeScript ناجح، Production build ناجح، وVitest: 54 اختباراً ناجحاً واختبار قاعدة بيانات حقيقي واحد متخطى لغياب `TEST_DATABASE_URL` المعزول. تم التقاط معاينة RTL لمساحة العمل. لا يُعتبر اختبار قاعدة البيانات الحقيقي مكتملًا قبل توفير قاعدة اختبار معزولة.

## تحديث تشغيل Manus — 2026-08-18

تم التحقق من اتصال قاعدة بيانات Manus المدارة بنجاح ووجود جداول الشركات والعقارات والمستأجرين والعقود والصيانة والمدفوعات التشغيلية. تمت معاينة `/workspace` و`/account` مباشرة، ونجح TypeScript والبناء و54 اختباراً، مع اختبار تكامل واحد متخطى لأنه يحتاج `TEST_DATABASE_URL` معزولاً.

تمت محاولة إنشاء قاعدة اختبار مستقلة باسم `dar_est_test` داخل خدمة قاعدة البيانات المدارة، لكن حساب المشروع لا يملك صلاحية `CREATE DATABASE`؛ لذلك لم يتم تشغيل CRUD التكاملية على قاعدة الإنتاج ولم تُستخدم بيانات تجريبية. سجل خطأ JSX الظاهر في السجلات قديم؛ بعد إعادة التشغيل والمعاينة الحالية يعمل المسار دون خطأ JSX نشط.

حالة التسليم: النسخة مرفوعة وتعمل في المسارات التي تمت معاينتها، لكن اختبار CRUD التكاملية المعزولة عبر حسابات فعلية لا يمكن اعتباره مكتملًا من دون صلاحية قاعدة اختبار منفصلة أو رابط `TEST_DATABASE_URL`.
