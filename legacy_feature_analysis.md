# تحليل مزايا المشروع القديم

## المزايا المكتشفة

المشروع القديم مبني على Flask مع قاعدة SQLite منفصلة للشركة، ويحتوي على لوحة Dashboard وOverview، إدارة العقارات مع الإضافة والتعديل والحذف والتفاصيل واستيراد Excel، إدارة العملاء مع استيراد العملاء وسجل العميل، المهام مع حالات وتبديل الحالة والحذف، المستأجرين، العقود، المدفوعات، الصيانة، التقارير، تقرير الحضور، إدارة المستخدمين، وسجل التدقيق.

## إجراءات ومسارات قديمة مهمة

تشمل المسارات القديمة `/properties`, `/add_property`, `/edit_property`, `/delete_property`, `/property/<id>`, `/upload_excel`, `/preview_import`, `/confirm_import`, `/sheets`, `/customers`, `/upload_customers`, `/customer/<id>/jard`, `/tasks`, `/toggle_task`, `/tenants`, `/contracts`, `/payments`, `/maintenance`, `/reports`, `/overview`, `/attendance_report`, `/manage_users`, `/audit_log`, و`/final_validation`.

## قواعد العمل القديمة

يدعم المشروع القديم أدوار admin وsupervisor وsales، ويستخدم صلاحيات مختلفة للقراءة والإنشاء والتعديل والحذف. يحتوي على حالات عقارات، حالات مهام new وin_progress وdone وcancelled، حالات عقود active وexpired وterminated، طرق دفع cash وbank وcard وtransfer، حالات صيانة open وin_progress وcompleted وcancelled، وأولوية low وmedium وhigh وurgent. كما يحتوي على إحصاءات المدفوعات المدفوعة والمعلقة، إجمالي المبالغ، الصيانة المفتوحة، الحضور اليومي، وتجميعات العقارات والمدفوعات والصيانة.

## قرار الدمج

المشروع الحالي يمتلك بنية SaaS متعددة الشركات وDrizzle/MySQL وtRPC وRBAC وخطط اشتراك، لذلك لن يتم نقل Flask أو SQLite أو الجلسات القديمة. سيتم نقل المزايا كوظائف أصلية داخل البنية الحالية، مع إضافة الجداول والإجراءات اللازمة، companyId في كل مورد، التحقق من الاشتراك والحدود، التوطين للغات الخمس، وسجل تدقيق للمسارات الحساسة. المزايا التي تحتاج نطاقاً جديداً مثل الاستيراد Excel والحضور والعقود والصيانة والمدفوعات والتقارير ستنفذ على مراحل بعد تثبيت نموذج البيانات وعزل الشركة.
